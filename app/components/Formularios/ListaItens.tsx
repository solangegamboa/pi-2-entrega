import React from "react";

type Props = {
    tipo: string
    items: object
    headers: object
    handleDelete: any
};

const ListaItens = (props: Props) => {
  return <div>ListaItens</div>;
};

export default ListaItens;
